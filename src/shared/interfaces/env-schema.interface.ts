export interface IEnvSchema {
  NODE_ENV: string;
  PORT: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_NAME: string;
  DB_HOST: string;
  DB_PORT: string;
  JWT_SECRET_KEY: string;
  JWT_TOKEN_EXPIRATION: string;
}
